<plexus>

	<load-on-start>
		<component>
			<role>org.sonatype.logging.Slf4jHandlerInstaller</role>
		</component>
		<component>
			<role>org.sonatype.plexus.jetty.ServletContainer</role>
		</component>
	</load-on-start>

	<components>

		<component>
			<role>org.codehaus.plexus.logging.LoggerManager</role>
			<implementation>org.codehaus.plexus.logging.slf4j.Slf4jLoggerManager</implementation>
			<configuration>
				<threshold>DEBUG</threshold>
			</configuration>
		</component>

		<component>
			<role>org.sonatype.plexus.jetty.ServletContainer</role>
			<implementation>org.sonatype.plexus.jetty.DefaultServletContainer</implementation>
			<configuration>
				<!-- To host multiple webapps, add root -->
				<!-- webapps>${webapp}</webapps -->
				<connectorInfos>
					<connectorInfo>
						<clazz>org.mortbay.jetty.nio.SelectChannelConnector</clazz>
						<host>0.0.0.0</host>
						<port>${applicationPort}</port>
					</connectorInfo>
				</connectorInfos>
				<webappInfos>
					<webappInfo>
						<warPath>${webapp}</warPath>
						<contextPath>/nexus</contextPath>
					</webappInfo>
				</webappInfos>
			</configuration>
		</component>

	</components>
</plexus>
