nexus.base.url=${nexus-application-protocol}://${nexus-application-host}:${nexus-application-port}/${nexus-application-name}/
nexus.control.port=${nexus-control-port}
nexus.bundle.dir=${bundle-expanded-dir}

proxy.repo.base.dir=${project.build.directory}/${proxy-repo-target-dir-name}/
proxy.repo.base.url=${proxy-repo-base-url}
proxy.repo.control.port=${proxy-repo-control-port}


foo=${config-expanded-dir}
bar=${nexus-basedir}