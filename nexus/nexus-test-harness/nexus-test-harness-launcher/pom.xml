<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.sonatype.nexus</groupId>
		<artifactId>nexus-test-harness</artifactId>
		<version>1.0.0-beta-4-SNAPSHOT</version>
	</parent>
	<artifactId>nexus-test-harness-launcher</artifactId>
	<name>Nexus Test Harness Launcher</name>
	<properties>
		<bundle-expanded-dir>target/bundle</bundle-expanded-dir>
		<config-expanded-dir>target/config</config-expanded-dir>
		<config-target-dir>${bundle-expanded-dir}/nexus-webapp-${project.version}/runtime/apps/nexus/conf</config-target-dir>
		<work-target-dir>${bundle-expanded-dir}/nexus-webapp-${project.version}/runtime/work</work-target-dir>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.sonatype.nexus</groupId>
			<artifactId>nexus-test-harness-config</artifactId>
			<version>${project.version}</version>
			<type>zip</type>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.sonatype.nexus</groupId>
			<artifactId>nexus-test-harness-work</artifactId>
			<version>${project.version}</version>
			<type>zip</type>
			<scope>provided</scope>
		</dependency>
		<dependency>
            <groupId>org.sonatype.nexus</groupId>
            <artifactId>nexus-webapp</artifactId>
            <version>${project.version}</version>
            <classifier>bundle</classifier>
            <type>zip</type>
            <scope>provided</scope>
        </dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
	    <filters>
	        <filter>src/test/filters/filter.txt</filter>
	    </filters>
	    <resources>
	        <resource>
	            <directory>${config-expanded-dir}</directory>
	            <filtering>true</filtering>
	            <targetPath>../../${config-target-dir}</targetPath>
	        </resource>
	    </resources>
		<plugins>
			<plugin>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.0</version>
				<executions>
				    <execution>
                        <id>unpack-bundle</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeArtifactIds>
                                nexus-webapp
                            </includeArtifactIds>
                            <outputDirectory>
                                ${bundle-expanded-dir}
                            </outputDirectory>
                        </configuration>
                    </execution>
					<execution>
						<id>unpack-config</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>unpack-dependencies</goal>
						</goals>
						<configuration>
							<includeArtifactIds>
								nexus-test-harness-config
							</includeArtifactIds>
							<outputDirectory>
								${config-expanded-dir}
							</outputDirectory>
						</configuration>
					</execution>
					<execution>
						<id>unpack-work</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>unpack-dependencies</goal>
						</goals>
						<configuration>
							<includeArtifactIds>
								nexus-test-harness-work
							</includeArtifactIds>
							<outputDirectory>
								${work-target-dir}
							</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>